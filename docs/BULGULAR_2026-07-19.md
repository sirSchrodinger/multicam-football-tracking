# Halısaha — Oturum Bulguları (18-19 Tem 2026)

Bu oturum yeni-saha calibrasyonunu **çözemedi**, ama nerede/neden takıldığını
tahmin değil **ölçümle** netleştirdi. Aşağısı gelecekte tekrar aynı duvarlara
toslamamak için dürüst kayıt.

---

## 1. DÜRÜST ÜRÜN DURUMU (bugün)
- **Per-venue-setup proof-of-concept.** cankaya (elle 16-nokta calibre edilmiş tek
  video-saha) üzerinde uçtan-uca **watchable** çalışıyor.
- **"Her sahayı otomatik" DEĞİL.** Zero-setup calib çözülemedi (aşağıda kanıt).

## 2. cankaya DELIVERABLE — DÜZELTİLDİ ve WATCHABLE
- Doğru calib: `calib/cankaya_cam2_FINAL.json` (16 yayılmış nokta, qa **6px**).
  (Oturumda bir süre yanlışlıkla eski taslak `cankaya_cam2_v2.json`/8-nokta/34×18
  kullanıldı → "touchline yanlış" sağası hep o dosya hatasıydı, etiketler değil.)
- Doğru boyut: FINAL template **36.5×17 m** (34×18 değil). Gerçek-metre çarpanı
  **×1.3252** (player-height metroloji, `scale_anchor`, **±16% yaklaşık, doğrulanmamış**).
- Kalite (GT-kimlik + median-5 yumuşatma):
  - **saha-dışı pozisyon %0.1** (eski zayıf-stitch'te %7.5)
  - **imkansız-adım %1.7** (yumuşatmasız %5.1; hedef <%2) → **watchable eşiği GEÇTİ**
  - tam-kapsam oyuncu mesafe med **121 m ±16%** (2 dk, makul casual oyun)
- Deliverable: `docs/cankaya_team_tactical.mp4` (sol video+takım-kutu | sağ şematik-2D,
  sürekli, kaleci=K), `docs/cankaya_team_report.jpg`, `docs/cankaya_team_territory.jpg`,
  `docs/cankaya_final_topdown.jpg`/`cankaya_onpaint.jpg` (kanıt).

## 3. TAKIM-AYRIMI — ÇÖZÜLDÜ (~0.87, dürüst)
- **Oyun modeli düzeltildi:** "sarı-vs-koyu" DEĞİL → **floresan YELEK (bib) vs kendi
  karışık kıyafeti** (kırmızı/siyah/mavi). Crude 2-means-on-renk bu yüzden battı.
- İki gözetimsiz sinyal (renk-bibfrac + emb-tight-cluster) füzyonu, güvenilir-GT'de **0.87**.
- Dürüstlük dersi: önce 0.91 dedim → bağımsız fresh-ajan denetimi benim GT'mde hata
  buldu (I22/I18 aslında bib) → GT-şişirme; gerçek 0.87. Sınıflandırıcı I22'yi conf 0.84
  ile güvenle yanlış → "saf yüksek-güven" iddiası da çürüdü. **Metrik yükseldiğinde önce
  GT'yi şüphelen; kendi GT'ni de bağımsız doğrula.**

## 4. WARPED BIRD'S-EYE = FİZİKSEL ÇIKMAZ
- Bağımsız 11-ajan denetimi: zero-setup warp **0/52 iyi** (41 partial, 11 bad).
  Alperen'in MANUEL calib'leriyle bile **5/49 iyi**.
- Kök-neden: **önplan file/tel + alçak-oblik near-field stretch** = kamera geometrisi;
  mükemmel calib bile düzeltemez (render-bug değil — m2px tek-remap ile doğrulandı).
- **DOĞRU YAKLAŞIM:** calib-doğrulama = çizgi-**orijinal-boyada** (verify_onpaint),
  warped-image DEĞİL. Ürün-görünümü = **şematik-2D** (metrik nokta + çizili saha).
  Haftalarca warped-image kovalamak ikimizi de yanılttı.

## 5. ★ PROJE AUDIT (4-ajan) — PIVOT KONFONDE VERİYLE VERİLMİŞ
- **"Keypoint modeli öldü / 49 saha yetmez" verdict'i KONFONDE.** Model, Alperen'in iyi
  etiketlerini (16-nokta cankaya + 24 el-doğrulanmış köşe `accurate_corners.json`) HİÇ
  kullanmadı. Kendi köşesini `calib_lines.json`'ı **distorted uzayda çizgi-kesişimiyle,
  barrel-distortion'u yok sayarak** türetti → el-etiketten **~200-800px** sapan çöp hedef.
  "94.6px öldü" büyük oranda etiket-gürültüsü, model-yetersizliği değil.
- **AMA iki-taraflı:** iyi-etiket kullanan `corner_net` bile unseen'de **overfit** (25px
  train, çöp unseen) → "veri-az/genelleşme" de kısmen gerçek. Ne "sadece re-label",
  ne "49 yetmez" tek başına doğru.
- seg U-Net İSTİSNA: tıklanan polyline'ı sadık raster'lıyor → genelleşiyor (IoU 0.538).

## 6. YENİ-SAHA CALIB — NEDEN ÇÖZÜLEMEDİ (ÖLÇÜLMÜŞ)
- **boundary-only seg-auto pozisyonları FINAL'den medyan 8.5m sapıyor** (Procrustes-hizalı,
  saha %18) → zero-setup stats **kullanılamaz**.
- **Hiçbir iç-özellik genelleşmiyor:** role-seg2 (çember/box/çizgi) unseen'de ÇÖP
  (`scratchpad/seg2_unseen.jpg`: çember 0px, center-kanalı tüm kareyi doldurur). Sadece
  2-kanal seg (sınır+iç) genelleşiyor — tek başına yetmez.
- **`joint_calib` Alperen'in ÇEMBER+BOX etiketini constraint YAPMIYOR** (sadece boundary+
  orta-çizgi; çember render'da, box tanıda). + L=34×W=18 tüm 49 sahaya **hard-code** (gerçek
  boyut yok sayılıyor → çember-R 1.3-3.2m çıkıyor). `joint_calib2.py`'de çember-constraint
  eklendi ama W/mutlak-ölçek belirsizliğinden **kararsız** (dejenere çıktılar).
- **VERİ-LİMİTİ: sadece cankaya'nın videosu var** → yeni-saha stats test bile edilemiyor.

## 7. DİĞER SİSTEM-DÜZEYİ BULGULAR (audit)
- **0.835 IDF1 maç-seviyesi değil:** küratörlü 18-id / 2-dk / tek-kamera penceresi; tam
  32-dk maçta top-16 kapsama **0.41'e çöküyor** (538 kimlik). GT ajan-adjudike (insan-gold
  değil) + tracker'ın tracklet-base'ini paylaşıyor → dairesellik. **İç-proxy, sertifika değil.**
- **Production zayıf koşuyor:** 0.835'lik OSNet `global_stitch` `run_pipeline.py`'a wire'lı
  DEĞİL; production hâlâ renk-histogram stitch kullanıyor.
- **Detection en güçlü bileşen:** ~%71-79 recall (yakın %100, uzak ~%68→%79 tiling), 0 false-pos.
- Son uçtan-uca kapı (kimlik-fix'ten önce): watchable=FALSE, 51/100 — sonra re-score yapılmadı
  (bu oturum GT-kimlikle watchable ölçtü: bkz §2).

## 8. FORWARD YOL (öncelik)
1. **Solver'ı Alperen'in iç-etiketlerini STABLE kullanacak yaz** (çember-constraint +
   per-venue-boyut + player-height mutlak-ölçek). ETİKET GEREKTİRMEZ (etiketler var);
   sonuç: tüm işaretli sahalar cankaya-kalitesi calib → per-venue ürün gerçekleşir.
2. **Zero-setup = araştırma:** yeni-saha VİDEOLARI topla (şu an 1 video) + çeşitli sahada
   iç-özellik etiketiyle genelleşen model. Bugün çözülemez; veri + zaman ister.
3. Hızlı düzeltmeler: joint_calib scale-hardcode fix, OSNet-0.835'i production'a wire'la,
   uçtan-uca re-score.

## DERSLER (tekrar etme)
- Sayıya değil GÖZE güven; metrik yükselince önce GT'yi şüphelen (Goodhart 3 kez tekrarladı).
- Warped-image doğru test/görünüm DEĞİL; lines-on-paint + şematik-2D.
- Stratejik karar (pivot) vermeden önce, kararı besleyen verinin doğru olduğunu doğrula.
- "Düzeldi" demeden önce TÜM videoyu/sahaları gör, tek kare değil.
