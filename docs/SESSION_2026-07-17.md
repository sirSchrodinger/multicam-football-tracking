# Halısaha otonom oturum — 17-18 Temmuz 2026

Alperen 5+ saat tam-otonom bıraktı ("evsunucu + lokal + RunPod, kendini tetikleyerek
geliştir, soru sorma"). Fable 5 limiti ~23:00'te doldu → Opus 4.8'e geçildi, devam.

## TL;DR — bu oturumun HEADLINE'ı
**İlk kez GT-tabanlı (proxy değil) kimlik ölçümü yapıldı ve mimari doğrulandı.**
Offline global OSNet-stitch **IDF1 = 0.863** (GT penceresi), BotSort 0.583, ham-tracklet
0.553. Yani memory'deki "%49 tavan" = **online-tracker artefaktıydı, appearance
bilgi-limiti DEĞİL** — daha önce sadece "span-proxy 0.49→0.64" diye ölçülmüştü, şimdi
insan-yargılı GT'ye karşı kanıtlandı.

## Yapılanlar (kanıtlı)

### 1. Hijyen sweep — AUDIT_ROADMAP 1-3 (commit 4c60f24)
- Üretim scriptleri (`selfimprove/loop.py`, `measure.py`, `eval/match_quality.py`,
  `eval/export_active.py`) yanlış `v2_vheight` calib yerine **FINAL** kullanıyor artık.
- `export_tracks`: tracker girişinde güvenli ball-drop (cls==0 & h/w<1.6), `zone` +
  `metric_lowconf` + `static_susp` kolonları, per-zone **METRİK** residual meta
  (px-QA Goodhart fix: far 6px "iyi" ama ~2.4m gerçek belirsizlik).
- 158 test yeşil, regresyon yok.

### 2. Kimlik-GT altyapısı + gerçek IDF1 (commit 85e9b78, 8566c8a, 1ecb1ea)
Pipeline (hepsi self-test'li):
- `eval/gtwin_tracklets.py` — konservatif yüksek-saflık tracklet üretici (over-segment
  OK, impurity yasak).
- `detect/global_stitch.py` — OSNet-medoid global stitch, **zaman-örtüşme HARD
  cannot-link** + hız-gate + appearance-gate; `stitch_fast` (öncelik-kuyruğu, **23×
  hızlı**, partition-birebir slow ile aynı).
- `eval/identity_eval.py` — gerçek IDF1/IDP/IDR + saflık + Frankenstein sayısı.
- **Agent-adjudicated GT** (2-tur workflow, ~1400 ajan görsel-yargı): tracklet kartları
  (crop montaj + saha-yörünge) → agent'lar her çift'i "aynı oyuncu mu" görsel karara
  bağladı, zaman-örtüşme cannot-link + overlap-guard union-find ile birleştirildi.
  - Round-1 dersi: 0.16 auto-gate over-merge yaratıyordu (29/57 pure) → impure gruplar
    0.08-altgruplara bölündü, adaptif-oylu round-2 (170 same / 114 different / 69 unsure).
  - Sonuç GT: **25 on-kimlik** (16 çok-birimli, Frankenstein-avı verify'dan geçiyor),
    overlap-guard 31 yanlış-birleşmeyi reddetti.

**IDF1 tablosu — verify SONRASI TEMİZ GT (18 doğrulanmış kimlik, 9.3k etiketli det):**
| yöntem | IDF1 | Frankenstein | frag/oyuncu | #id |
|---|---|---|---|---|
| ham tracklet (taban) | 0.515 | 0 | 4.94 | 89 |
| **offline stitch g0.16/mg20** | **0.835** | **0** | 2.11 | 32 |
| offline stitch g0.13/mg20 | 0.835 | 0 | 2.22 | 35 |
| offline stitch g0.20/mg20 | 0.835 | 1 | 1.94 | 26 |

- BotSort (25-id GT, verify öncesi): IDF1 0.574, **frank=11** (online tracker çok daha
  fazla çapraz-kimlik kirlenmesi üretiyor).
- **Kritik: en iyi config'te SIFIR Frankenstein** — doğrulanmış-temiz kimliklere karşı
  offline stitch çapraz-kimlik kirlenmesi üretmiyor. Kalan hata = parçalanma (frag 2.11),
  yani birleştiremediği tracklet'ler (temkinli), yanlış-birleştirme DEĞİL.
- Verify öncesi (25-id kirli GT) rakamları daha düşüktü (0.801) çünkü GT'nin kendisi
  7 Frankenstein içeriyordu. Temiz GT dürüst tabloyu veriyor.

### 3. RunPod tam-maç export (iron-guard'lı, $0.22/h 3090)
- 32-dk gerçek-maç penceresi (t=1500-3420) **12.5fps** base+tiling export:
  **262.696 tespit**, 10.9/kare, tiling +5.1k uzak-tespit.
- Iron-guard kusursuz: EXPORT_DONE → parquet indir → pod TERMINATE → doğrula. Harcama
  ~$0.30, pod:null teyitli.
- Tuzak dersleri: torch 2.11+cu128 pin (DTensor import hatası), setup **script-dosyası**
  olmalı (iç-içe tırnak /bin/sh'i kırıyor), ckpt yurt-ağında yavaş → pod HF'den çeker.

## Dürüst negatifler
- **3fps tam-maç prototipi yetersiz:** clean_match (76k det) → 763 kimlik, top-16
  kapsam %29, span≥10dk sadece 13. 12.5fps ŞART (3fps'te association kopuyor).
- **in_pitch=%100:** FINAL homografi kadraj-dışını içeri katlıyor (bilinen; aggregate
  için düşük-etki, kimlik image-space çalıştığı için sorun değil).

## Ev sunucusu — ERİŞİLEMEZ (dış etken)
Kutu ~18:40'tan beri iki bağımsız outbound kanaldan da ölü: Tailscale control-plane
"last seen 18:40" + cloudflared `chat-bridge.sirschrodinger.com` HTTP 530. Benim tarafım
sağlıklı (Tailscale UDP açık). İki kanal aynı anda ≥6h ölü + RTC self-heal açmamış →
elektrik/internet kesintisi (Bursa). Uzaktan çözülemez; aileden prize/routera bakması gerek.

## Sıradaki
- Verify → disputed'ları GT'ye işle → FINAL IDF1 (verify sonrası).
- Tam-maç 12.5fps: pitch+emb → tracklet → stitch_fast → 32-dk temsili per-oyuncu.
- Aggregate demo v2 (IDF1 headline + tam-maç kapsam).
